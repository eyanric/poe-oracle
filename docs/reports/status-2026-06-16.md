# ORACLE — status snapshot

**Date:** 2026-06-16 · **League:** Mirage (3.28) · **Gates:** typecheck ✓ · lint ✓ · 418 tests ✓ · build ✓ · parity byte-identical. Pushed to `origin/main`.

Read-only, ToS-clean, MIT/clean-room PoE 1 copilot (MCP server), layered `tools/ → services/ → data/`.

## Where it stands

- **End-to-end usable through the MCP tools.** Front door: `resolve_target` (stat → candidate mod identities) + `solve_craft` (accepts human stat queries → ranked plans, or a disambiguation response when a stat maps to several identities — never guesses).
- **Method library complete** across the modeled set: currency/essence/fossil/bench+meta/harvest/recombinator/eldritch/influence/catalyst/anoint/veiled/synthesis/memory-strands — all on one `CraftModule` interface.
- **Path Solver** (the differentiator): single-method spine → branch-and-bound multi-step search → producer index (influence/eldritch/veiled/anoint/synthesis) → unprotected cross-step reproduction. Ranks risk-adjusted plans + craft-vs-buy vs live specific-variant price.
- **Track B** (league-start intel for 3.29): skeleton standing, dry-run green.

## Recent increments (this session)

- **Data generation via poewiki Cargo** (browser-UA fetch — now a reliable Code-fetchable source): full **470-recipe anoint table** + the **synthesis implicit pool** (per item class). Closed the last "not in any export" gaps.
- **Method-module validation sweep:** all 11 modules audited vs the current wiki — 1 stale-fixed (harvest amounts), 9 confirmed-inert, 4 availability calls flagged for Eric.
- **Harvest `remove` kind dropped** (confirmed gone from the game).
- **`parse_pob` real-export validation:** 6-build real corpus (pobb.in) across archetypes (CI/ES · bow+melee attack · spell · RF · 166-item · deep progression). **5 parser bugs found & fixed**; the crux holds — every PoB `<PlayerStat>` == our extracted value. Data-driven regression net (`pobCorpus.test.ts`).

## Standing flags / for Eric

- **3.29 re-gate checklist** (reveal ~Jul 16, launch ~Jul 24): recombinators availability; Mirage content rotating out (Crystallised Rancour reforges, Sinistral/Dextral catalysts).
- **Bench costs** unverified vs current patch (override seam in place); **synthesis count dist** 75/19/6 is datamined (no better source); **Vivid Vulture** price is a manual hook (beast not in the feed).
- **Tooling note:** raw ~6 KB+ PoB codes truncate when hand-vendored — use pobb.in IDs (fetched exact bytes).

## Next (your call)

Minion-build PoB fixture (rounds out archetypes) · Tier-3 breadth (beast / corruption / resonator combos) · Track B / 3.29 launch prep · UI per-mod picker (consumes the existing `resolve_target`/`solve_craft` contract).
